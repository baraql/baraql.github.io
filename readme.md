# Setup before anything
install dependencies:
```npm i```

# To run
in parent folder:
```npm start```

# To deploy
commit & push first (to master), then run this in parent folder:
```npm run deploy```